# 07 交付与验收（门禁：发布）

## 输入
- 以上所有阶段产物

## 产物（必须交付）
- 启动方式：FastAPI 入口、环境变量、依赖清单
- 验收用例：赛程入库、直播文本入库、分词结果、语义关系入库与查询
- 回滚策略：DDL/DML 版本化，必要时提供反向脚本
- 数据回放：从 raw 重新跑解析与抽取，结果可对比
- 工具替换说明：记录 `jieba` → PaddleNLP `TaskFlow` 的调用差异、依赖变更与兼容策略

## 检查清单
- 新环境一键初始化：DDL + seed insert 可执行
- 关键链路可用：采集 → 入库 → 抽取 → 查询
- 直播文本分词验收通过：球员姓名按词典整体切分、停用词被过滤、分页/404/比赛结束规则保持不变
