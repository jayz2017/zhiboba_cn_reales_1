# 03 API 设计（门禁：接口评审）

## 输入
- 领域模型与事件流

## 产物（必须交付）
- 资源与路由：/api/v1/*（版本化）
- 错误模型：统一 error_code + message + trace_id（预留）
- 数据契约：请求/响应字段、示例、边界值
- 安全预留：鉴权（JWT/API Key）、限流、审计字段

## 约束
- 只暴露资源，不暴露内部表结构
- 任何写入操作必须幂等（提供幂等键或自然唯一键）

## 检查清单
- 所有接口有状态码约定与错误码
- 超时/重试策略不由客户端猜测，必须在文档中明确
